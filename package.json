{
  "name": "tsukiyomi-platform",
  "private": true,
  "version": "3.0.0",
  "description": "Tsukiyomi monorepo - Flutter client + NestJS API",
  "author": "Piyush Mishra",
  "license": "MIT",
  "scripts": {
    "server:install": "npm install --prefix apps/server",
    "server:build": "npm run build --prefix apps/server",
    "server:dev": "npm run start:dev --prefix apps/server",
    "server:test": "npm test --prefix apps/server",
    "server:seed": "npm run seed --prefix apps/server"
  }
}
